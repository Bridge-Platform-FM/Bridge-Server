<!-- claude-pr-review: eeca3a2c4cf1515a4e74a4824da6d798f6014b3e -->
> 🤖 **AI-generated code review** — produced by Claude. Treat it as a starting point for review, not a substitute for one.

## What changed

### Feature Changes
This PR is meant to deliver a "Matching Engine Dashboard" for admins — a `GET /api/v1/admin/matching-engine/stats` endpoint returning KPIs like connection acceptance rate, match volume, average compatibility score, sector mix, and rule-based vs. ML algorithm mix — plus a companion `POST /api/v1/matching/events` endpoint intended for the frontend to log match interactions (shown/skipped/flagged/connected/opened deal room). Bundled into the same PR is a large, unrelated migration of primary/foreign key IDs from integers to UUIDs across nearly every model, plus minor ESLint and cookie-handling touch-ups.

### User-facing impact
- Admins get a new dashboard KPI endpoint (`GET /api/v1/admin/matching-engine/stats`), reachable and working as coded.
- The frontend "explore" match-interaction logging endpoint (`POST /api/v1/matching/events`) **is not reachable** — `matchingEventController.logMatchEvent` exists but `src/routes/matchingRoutes.js` was never updated to mount it. Nothing will log until a follow-up wires the route.
- `GET /api/v1/users/role-details` (`getUserRoleDetails`) is **broken by this PR**: `userId`/`companyId` are now read as raw UUID strings but the existing validation still does `isNaN(userId)` / `userId <= 0`, which is always true for a non-numeric UUID string — every request with a valid UUID will now incorrectly be rejected with `USER_ID_REQUIRED`/`COMPANY_ID_REQUIRED`. This is a regression of a previously-working endpoint.
- Every ID (`user.id`, `company.id`, foreign keys) moves from integer to UUID — every response shape containing an ID changes from a number to a string.

### Risks associated
- **Scope creep**: a dashboard/analytics feature is bundled with a full INTEGER→UUID primary-key migration across ~20 models. These are unrelated changes with very different risk profiles and should not ship in the same PR — the UUID migration is a major, cross-team change affecting every downstream integration and the Next.js frontend (numeric IDs become strings).
- Migrations are gitignored in this repo (verified: no `migrations/` directory present locally), so the actual SQL for the ID-type conversion is not part of this diff — reviewers cannot verify it exists, has been tested against non-empty tables, or has a backfill/rollback plan.
- A checked-in file, `.claude-reports/pr-82.md`, contains a previous AI-generated review of this PR that makes several claims **not supported by the code as it currently stands** (duplicate `const` declarations causing boot failure, an undefined-variable crash in `companyRegistration`, a missing `sequelize` import breaking `updateUserLimitConfig`, a duplicated route in `adminRoutes.js`). I checked each of these against the actual repository state and none of them reproduce — the file appears to describe an earlier, since-fixed revision of this PR (or is simply stale/inaccurate) and should not be trusted as-is; it's also arguably not something that belongs committed to the repo. Flagging this so reviewers don't act on stale findings.

### Decisions needed
- Should the UUID migration be split into its own PR with its own migration/rollback plan, separate from the matching dashboard feature? (Recommended: yes.)
- Is there a maintenance/migration plan for converting existing production integer IDs to UUIDs without breaking the live frontend or any external integrations relying on numeric IDs?
- Should `.claude-reports/pr-82.md` be removed from the PR (checked-in AI review output, and per the analysis above, materially inaccurate)?
- Was `POST /api/v1/matching/events` meant to ship in this PR? As-is it's dead/unreachable code — either wire the route or hold the controller for a follow-up.

## Code changes

### Suggested Reviewers
This PR's commits are authored by **Ganesh-Phutane** and **Manish**.

Additional people worth looping in based on prior history in touched areas:
- Nearly every touched model (`Company`, `User`, `DealRoom*`, `KycInfo`, etc.), `adminController.js`, `authController.js`, `adminMiddleware.js`, `adminRoutes.js`: **abhinavFM** has by far the deepest and most recent history (e.g. 16 commits on `authController.js`, 9 on `adminController.js`, 8 on `Company.js`) — given the scale of the ID-type change, they should review.
- `adminController.js` / `adminService.js` / `adminRoutes.js`: Ganesh-Phutane and Manish also have prior commits here in addition to abhinavFM.
- `Faq.js`, `Meeting.js`, `SubscriptionPlan.js`, `UserLimitConfig.js`, `UserSession.js`, `UserSubscription.js`: Manish has prior history.
- `eslint.config.js`: dev-gaurav and abhinavFM each have one prior commit.
- `matchingEventController.js`, `MatchingEvent.js`, `matchingEventRepository.js`, `.claude-reports/pr-82.md`: genuinely new files, no prior history — first-time review needed from scratch.

### What changed
- **Matching Engine Dashboard**: new `MatchingEvent` model/table, `matchingEventRepository.logMatchingEvent`, `matchingEventController.logMatchEvent`, and 8 raw-SQL aggregate functions in `adminRepository.js` consumed by `adminService.getMatchingEngineStats()` and exposed via `adminController.getMatchingEngineStats`.
- **ID type migration**: `id` and all `*_user_id`/`*_company_id`/`created_by`/`updated_by`/`deleted_by`/etc. columns changed `DataTypes.INTEGER` → `DataTypes.UUID` across `User`, `Company`, `CompanyUserRole`, `DealRoom*`, `KycInfo`, `Meeting`, `SubscriptionPaymentTransaction`, `UserConnection*`, `UserLimitConfig`, `UserSession`, `UserSubscription`, plus various master/log tables.
- Deletion of `src/models/base/{AuditFields,SoftDeleteFields,TimestampFields}.js` — these appear to be unused shared mixins (not referenced by any model I checked) removed as part of cleanup; worth confirming nothing else imports them.
- `adminController.getUserLimitConfig`/`updateUserLimitConfig`: userId parsing changed from `parseInt`+`isNaN` to a UUID regex — correctly updated, consistent with the model change.
- `userController.getUserRoleDetails`: userId/companyId parsing changed from `parseInt` to raw string, but validation was **not** updated to match (see Critical Risks).
- Dead-code cookie helpers (`ACCESS_COOKIE_OPTS`/`REFRESH_COOKIE_OPTS`/`setAuthCookies`) added to both `authController.js` and `adminController.js`, duplicating `src/utils/token.js`'s existing `cookieOptions`/`COOKIE_NAMES`. In `authController.js` the one call site is commented out; in `adminController.js` `setAuthCookies` is defined but never called anywhere. This is inert but confusing dead code, not a live bug.
- ESLint: ignore pattern for generated `migrations/*_noname.js`, and `quotes` rule relaxed to allow template literals.

### API contract changes
- **New route**: `GET /api/v1/admin/matching-engine/stats` in `src/routes/adminRoutes.js:45`, guarded by `adminMiddleware` only — no Joi schema (acceptable, no input) and no explicit permission check (there's a `// TODO: add permission for this route` comment at `adminRoutes.js:44`, unlike every other admin route which uses `authorize(PERMISSIONS...)`).
- **`matchingEventController.logMatchEvent` is not mounted anywhere.** `src/routes/matchingRoutes.js` only has `GET /profiles`; the documented `POST /api/v1/matching/events` route does not exist. Dead code as shipped.
- **Response shape change**: every endpoint returning a user/company/deal-room ID now returns a UUID string instead of a number — a breaking contract change for any existing frontend/integration consumers, not called out in the PR description.
- **Model changes**: new `MatchingEvent` model, plus the ID-type change across the ~20 models listed above. **`migrations/` is gitignored in this repo (confirmed — no migrations directory present) — none of these column-type changes has a corresponding migration in this diff.** Reviewers must confirm a matching migration exists outside this PR, has a data-backfill strategy for populated tables, and has been applied/tested per environment before this ships anywhere shared.

### Impact Analysis
- `matchingEventRepository`/`matchingEventController` have no route wiring — currently dead code with no caller in the app.
- `userController.getUserRoleDetails` (`src/controllers/userController.js:139-157`) is broken by this PR (see Critical Risks) — this is an **existing, previously-working endpoint** now regressed, not new-feature risk.
- `adminRepository.getZeroEngagementProfiles`/`getMatchingEngineKpis` (`src/repositories/adminRepository.js`) join `user`, `company`, `company_user_role`, `company_role_master`, `user_connection`, `deal_room` via raw SQL. Since the ID-type migration touches all of these tables, the raw SQL and the ORM-driven repositories must be migrated in lockstep — any table not converted alongside its joined peers will produce type-mismatch errors or silently wrong joins at the DB level. This diff appears internally consistent (all referenced tables were converted), but there's no automated check enforcing that.
- `src/models/base/{AuditFields,SoftDeleteFields,TimestampFields}.js` deletion — I found no remaining references to these files in models; low risk, but worth a final grep before merge in case something outside the diff's scope still requires them.
- `src/tests/routes/adminRoutes.test.js` (existing, prior history: abhinavFM/Ganesh-Phutane/Manish) mocks `sequelize.query`/`transaction` and inspects the route stack — it was not updated for the new `/matching-engine/stats` route.

### Critical Risks
1. **`userController.getUserRoleDetails` is broken.** `src/controllers/userController.js:141-156`: `userId`/`companyId` are now raw UUID strings, but the guard clauses still do `isNaN(userId) || userId <= 0` (and same for `companyId`) — `isNaN()` on a non-numeric UUID string is always `true`, so **every request will be rejected as `USER_ID_REQUIRED`/`COMPANY_ID_REQUIRED` regardless of validity.** This regresses an existing, working endpoint to permanently unusable.
2. **UUID migration correctness/data-safety** on live tables is unverifiable from this PR alone — the actual `ALTER COLUMN` migration and any ID-mapping/backfill strategy live outside this gitignored-and-thus-invisible `migrations/` directory.
3. **Unbounded/uncached raw SQL** in `adminRepository.js`: `getZeroEngagementProfiles` (`src/repositories/adminRepository.js`) does a `NOT IN (subquery UNION subquery)` over `user_connection` with no pagination beyond `LIMIT 100`, and `getMatchingEngineStats` fires 8 of these raw queries in parallel per request. Fine at current data volume; no caching/indexing story for growth — flag for follow-up, not a blocker.
4. `matchingEventController.logMatchEvent`'s catch block always returns HTTP 200 ("Event skipped.") on failure — reasonable for fire-and-forget analytics, but it means callers/monitoring will never see failures except via `errorLogger`, and this is currently moot since the route isn't mounted.
5. The `.claude-reports/pr-82.md` file bundled in this diff describes app-boot-breaking bugs that do not reproduce against the actual code — do not act on that file's specific line-level claims without re-verifying, as done here.

### Convention violations & inconsistencies
- **Layering violation**: `matchingEventController.logMatchEvent` (`src/controllers/matchingEventController.js:28-63`) calls `matchingEventRepository` directly, skipping the service layer entirely. There is no `matchingEventService`, and the controller never goes through `ServiceResponse` — violates the routes→middleware→controllers→services→repositories layering and the "services always return ServiceResponse" rule.
- **`process.env` used directly** instead of `src/configs/env_configs.js`: `ACCESS_COOKIE_OPTS`/`REFRESH_COOKIE_OPTS` in both `authController.js:16,22` and `adminController.js:16,22` read `process.env.NODE_ENV` directly. This is dead code (see below) but still a convention violation if ever activated, and it drops the existing `crossSiteCookieFlags()` cross-site cookie support from `src/utils/token.js:26-29`.
- **Enums/constants**: hardcoded cookie names `'access_token'`/`'refresh_token'` in the dead `setAuthCookies` helpers instead of `COOKIE_NAMES`. `matchingEventController.js`'s `VALID_ACTIONS` array (lines 7-13) duplicates the `MATCHING_EVENT_ACTIONS` enum already defined in `src/models/MatchingEvent.js:8-14` — should import and reuse instead of a parallel hardcoded list.
- **Missing `authorize(PERMISSIONS...)`**: `GET /matching-engine/stats` (`adminRoutes.js:45`) is the only admin route in this file without a `PERMISSIONS`-based `authorize()` check (there's a literal `// TODO: add permission for this route` comment above it) — inconsistent with every other route in the file.
- `adminService.js:158` — `module.exports` is placed before `getMatchingEngineStats` is defined further down the file. Works correctly at runtime (function declarations hoist), but is an unusual structure versus every other file in the repo where `module.exports` is last; worth tidying for readability.
- Dead code: `ACCESS_COOKIE_OPTS`/`REFRESH_COOKIE_OPTS`/`setAuthCookies` in both controllers are unused (or, in `authController.js`, only reachable via a commented-out call) — should be removed rather than left as inert duplication.

### Code quality issues
- `src/controllers/matchingEventController.js` — missing trailing newline; several other touched files also lost trailing newlines (`authController.js`, `adminMiddleware.js`, most models) — cosmetic but inconsistent with the rest of the diff's own formatting.
- `src/repositories/matchingEventRepository.js:17-24` — trailing commas in the `logMatchingEvent` destructured parameter defaults (e.g. `action = 'shown',`) — violates this repo's own `comma-dangle: never` ESLint rule (unchanged by this PR's ESLint edits).
- `src/controllers/userController.js:145,152` — dead/incorrect validation logic post-UUID-migration (see Critical Risks #1); should be rewritten as a UUID-format check like the one added in `adminController.js`.
- `src/services/adminService.js:158` — `module.exports` placed mid-file, before the function it exports is defined; confusing to read even though it works.

### Security issues
- No new file upload endpoints — ClamAV/multer conventions not implicated.
- No raw-SQL string concatenation from user input found: all new `adminRepository.js` queries are either parameter-free aggregates or (in `matchingEventRepository.js`) use `sequelize.query` with `:replacements` — no SQL injection risk identified.
- `matchingEventController.logMatchEvent` correctly derives `userId` from `req.userId` (authMiddleware-attached), not from the request body — no authorization-via-client-data issue there.
- `GET /matching-engine/stats` has no fine-grained `PERMISSIONS` check (just `adminMiddleware`), unlike every sibling admin route — worth a decision on whether any admin who passes `adminMiddleware` should see these KPIs, or whether it should be gated like the rest.
- The dead `setAuthCookies`/`ACCESS_COOKIE_OPTS` code, if ever wired up as originally seems intended in `companyRegistration` (the commented-out call at `authController.js:91`), would set auth cookies before MFA/OTP verification completes — worth flagging now so it isn't accidentally un-commented later without a security review, even though it's inert today.

## Testing required

### Test coverage
No new or updated tests are included in this diff. Missing:
- `src/tests/controllers/matchingEventController.test.js` (new) — validation branches (missing `matchProfileId`, invalid `action`), success path, and the swallow-all-errors→200 behavior.
- Extension to `adminRepository`/`adminService` tests for the 8 new raw-SQL query functions and `getMatchingEngineStats` (no `src/tests/services/adminService.test.js` currently exists).
- `src/tests/routes/adminRoutes.test.js` (existing, owned per history by abhinavFM/Ganesh-Phutane/Manish) should be extended to assert `GET /matching-engine/stats` wiring.
- No existing test would have caught the `getUserRoleDetails` regression — a `userController.test.js` covering that endpoint with a UUID `userId`/`companyId` does not appear to exist and should be added.
- `npm run test:matching` suite (eligibility/geoFilter/ranking/scoring/matchingApi) is unrelated to the new event-logging/dashboard code and doesn't cover it.

### Testing scenarios
1. `GET /api/v1/users/role-details?userId=<uuid>&companyId=<uuid>&roleId=<n>` as an authenticated user — **expect this to currently fail** with `USER_ID_REQUIRED`/`COMPANY_ID_REQUIRED` due to the `isNaN` bug; confirm after a fix that it returns 200 with correct role details. (Author: Ganesh-Phutane/Manish; prior history on this controller: abhinavFM, dev-gaurav.)
2. `GET /api/v1/admin/matching-engine/stats` as an authenticated admin — expect 200 with all documented fields, including correct `0`/`null` defaults on an empty/seed database (no `matching_events` rows). (Authors: Ganesh-Phutane/Manish; abhinavFM has the most history on `adminController.js`/`adminRoutes.js` for a second opinion.)
3. `GET /api/v1/admin/matching-engine/stats` without a token / with a non-admin token — expect 401/403 via `adminMiddleware`; also confirm intentionally whether any admin (vs. only certain roles) should see this data, given the missing `authorize()` check.
4. `PUT /api/v1/admin/users/:userId/limit-config` with a valid UUID `userId` — confirm the transaction-wrapped update still commits/rolls back correctly post-migration. (Authors: Ganesh-Phutane/Manish; abhinavFM/Manish have prior history.)
5. Attempt `POST /api/v1/matching/events` — expect 404 since the route isn't mounted; confirm with the author whether this was meant to ship now or in a follow-up.
6. Any endpoint returning `user.id`/`company.id` (login, `GET /users/:id`, etc.) against a Next.js frontend build expecting numeric IDs — confirm frontend compatibility before merging the UUID change to a shared environment.

### Edge Cases
- `getUserLimitConfig`/`updateUserLimitConfig` UUID-regex validation — test a legacy-style numeric ID (should now be rejected), uppercase UUID (regex is case-insensitive, should pass), and a malformed UUID (missing dashes, should 400).
- `getUserRoleDetails` with a well-formed UUID `userId` — currently always fails per the bug above; also test missing/empty `userId`/`companyId` to confirm the "required" message still fires for the right reason once fixed.
- `logMatchEvent` (once wired) with an `action` outside `VALID_ACTIONS`, a missing `matchProfileId`, and a non-numeric `compatibilityScore` (`Number('abc')` → `NaN`, currently written to the DB unguarded by any Joi schema).
- Matching-events aggregate queries on an empty `matching_events` table — confirm `0`/`null` defaults render correctly rather than causing frontend crashes on `undefined`.
- Role-based access to `GET /matching-engine/stats` across super admin / admin roles, given the missing explicit permission check.

### Critical Risks
1. **`getUserRoleDetails` regression** — highest priority; a previously-working endpoint is now unconditionally broken for any real (UUID) input.
2. **UUID migration data-safety** on non-empty production tables — the actual migration is outside this PR and gitignored; must be independently verified before any shared-environment deploy.
3. **Unreachable matching-events endpoint** — confirm intent (ship now vs. follow-up) before merge, since it's currently non-functional.
4. **Missing permission check on the new admin stats route** — confirm whether this is an intentional gap or an oversight before shipping.
5. The **checked-in `.claude-reports/pr-82.md`** describes bugs that don't reproduce in the current code — reviewers should disregard its specific claims and rely on direct verification (as done in this report) rather than treating it as ground truth.
